name: Tests

on:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Restore
        run: dotnet restore ArgosSharp.sln

      - name: Build
        run: dotnet build ArgosSharp.sln --configuration Release --no-restore

      - name: Test
        run: dotnet test ArgosSharp.sln --configuration Release --no-build